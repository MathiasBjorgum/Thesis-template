\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages}[2020/09/15 Packages]
\usepackage[OT1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{booktabs, tabularx, longtable}
\usepackage{svg}
\setlength {\marginparwidth }{2.5cm} % Lagt til for å sikre marg
\usepackage{todonotes}
\usepackage{dirtytalk}
\usepackage{changepage}
\usepackage{pdfpages}
\usepackage{tocloft}
\usepackage{array}
\usepackage{dirtytalk}


% ------- Innhold -------
% Generelt
% Vedlegg
% Bibliografi og referanser
% Sideoppsett
% Plotting
% -----------------------

% ----- Generelt -----
\usepackage{times} % Setter fonten til times new roman
\usepackage[british, main =norsk]{babel} % Setter norsk språk
\usepackage{import} % Gjør det mulig å importere deler
\usepackage{hyperref} % Gjør det mulig å ha hyperlenker
\hypersetup{    % Setter farger på linkene
    colorlinks,
    citecolor = black,
    filecolor = black,
    linkcolor = black,
    urlcolor = black
}

\def\equationautorefname{formel}  % Autoref-name for formler
\def\figureautorefname{figur}  % Autoref-name for figurer
\def\tableautorefname{tabell}    % Autoref-name for tabeller
\def\sectionautorefname{kapittel}    % Autoref-name for kapitler
\def\subsectionautorefname{delkapittel} % Autoref-name for delkapitler
\def\subsubsectionautorefname{delkapittel}

\usepackage{url} % setter formatering for url
\urlstyle{sf}


\usepackage{graphicx}   % Håndterer bilder
\usepackage{wrapfig}    % Gjør det mulig å ha tekst rundt et bilde
\usepackage{float}      % Tvinger floats til å være et bestemt sted med "H"

\usepackage{siunitx} % Legger til SI enheter

% Pakkene under her bidrar til at matte ser bedre ut
\usepackage{bm}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}

\setsecnumdepth{subsection}
\settocdepth{subsection}


% ----- Vedlegg -----
\usepackage{appendix}
\newcommand{\addappendix}{   % Egendefinert funksjon for å sette inn vedlegg med gitte innstillinger
    \newpage
    \appendix
    \section*{Vedlegg} % Navn på vedlegg
    \addcontentsline{toc}{section}{Vedlegg} % Legger vedlegget til innholdsfortegnelsen
    \renewcommand{\thesubsection}{\Alph{subsection}} % Endrer numrering til store bokstaver
}

% ----- Bibliografi og referanser -----
\usepackage{csquotes}
\usepackage[backend = biber, % Setter sorteringsmåten
            style = authoryear-comp, % Harvard-stilen
            urldate = long, % Long: 24th Mar. 1997 | Short: 24/03/1997
            maxcitenames = 2, % Setter maks antall navn før det blir "et al"
            ]{biblatex}
\addbibresource{references.bib} % Legger til referansene fra referansefila
\renewcommand*{\nameyeardelim}{\addcomma\space} % Setter komma mellom forfatter og år

\usepackage[labelfont=bf]{caption}
\usepackage{subcaption}
\newcommand{\source}[1]{\vspace{-4pt} \caption*{\hfill \footnotesize{Datakilde: {#1}} } }   % Egendefinert kommando for å sette inn kilder til bilder

% ----- Sideoppsett -----
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}} % lager en fet linje
\linespread{1.3} % Setter linjeavstand til 1.5

\usepackage{parskip} % Under her settes reglene for linjeskift og avsnitt
\setlength{\parindent}{1.5em} % Nytt avsnitt begynner med dette innrykket (standard 1.5em)
\setlength{\parskip}{0em} % Nytt avsnitt begynner denne distansen unna forrige (standard 1em)

% Innstillinger for hvor på siden teksten ligger
\usepackage[a4paper,
    includeheadfoot,
    top = 20mm,
    bottom = 20mm]{geometry}
    
% --- Ta med dette ved tosidig ---
\geometry{
    a4paper,
    includeheadfoot,
    inner = 30mm,
    bottom = 20mm,
    top = 20mm,
    outer = 25mm}
    
% --- Ta med dette dersom det er ensidig ---
% \geometry{
%     a4paper,
%     includeheadfoot,
%     left = 25mm,
%     bottom = 20mm,
%     top = 20mm,
%     right = 25mm}


% Bruker fancy header

\usepackage{fancyhdr}
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE, OR]{\leftmark}
\fancyhead[RE, LO]{\rightmark}
\renewcommand{\headrulewidth}{0.1ex}
\renewcommand{\footrulewidth}{0.1ex}
\fancyfoot[C]{\thepage}

% ----- Plotting -----
% Gjør det mulig å plotte grafer
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\pgfplotsset{compat = 1.8}
% \pgfplotsset{width=14cm, height=7cm,compat=1.8} % Ta bort '%' for å ha en standardstørrelse for alle plots
\usepackage{mathrsfs}
\usetikzlibrary{arrows}
